GENZ EDU TECH — CLEAN DEPLOYMENT PACKAGE

This folder is a frontend-only, static production build.

NETLIFY:
- Build command: leave EMPTY
- Publish directory: .
- Deploy this folder/repository as-is.

GITHUB:
- Create a NEW repository.
- Upload the CONTENTS of this folder to the repository ROOT.
- Do NOT upload the old .git folder or the old Replit workspace.
- The website is already built, so Netlify does not need pnpm/npm to build it.

DOMAIN:
- After deployment, connect genzedutech.in in Netlify.
- Then update the DNS records in GoDaddy as Netlify instructs.

An editable source project is included in /source-project.
